{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.1.3",
    "moment": "^2.22.1",
    "moment-timezone": "^0.5.25",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.2.2",
    "jest-cli": "^26.6.3"
  },
  "engines": {
    "node": "12"
  }
}
